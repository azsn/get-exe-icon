`get-exe-icon` was originally made for a Node project (using [yarn](https://yarnpkg.com)).
This directory contains the Node bindings and configuration needed to build
`get-exe-icon` into a Node module.
